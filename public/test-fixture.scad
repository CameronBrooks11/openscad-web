// E2E test fixture — deterministic local file served by the dev/prod server.
// Replaces the external GitHub URL in the "load a demo by url" e2e test so
// the suite has zero external network dependencies.
// Expected output: "Top level object is a 3D object (manifold):"
sphere(10);
